# Ein Name für den Workflow
name: Conda SageMath CI Pipeline

# Trigger: Starte bei jedem Push und Pull Request
on: [push, pull_request]

jobs:
  run-conda-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}

    steps:
      # 1. Schritt: Code aus dem Repository laden
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Schritt: Miniconda-Umgebung einrichten
      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-activate-base: false
          # 🎯 HIER DIE PYTHON-VERSION AUF 3.11 GEÄNDERT
          python-version: "3.11"

      # 3. Schritt: Conda-Kanäle konfigurieren (macht die Installation stabiler)
      - name: Configure Conda channels
        run: |
          conda config --set channel_priority strict
          conda config --add channels conda-forge
      
      # 4. Schritt: Conda-Umgebung erstellen und Abhängigkeiten installieren
      - name: Create Conda environment and install dependencies
        run: |
          # 🎯 HIER EBENFALLS PYTHON 3.11 ANGEGEBEN
          conda create -n sage-env python=3.11 sagemath gpytorch pytest -y
      
      # 5. Schritt: Tests mit pytest ausführen
      - name: Run tests
        run: |
          conda activate sage-env
          pytest